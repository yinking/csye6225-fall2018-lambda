install: true

script: true

after_success:
- pip install awscli --upgrade --user
- zip -r myLambda.zip myLambda
- pwd
- aws lambda update-function-code --function-name myLambdaFunction --zip-file fileb://myLambda.zip --s3-bucket $lambda_functions_s3_bucket --s3-key myLambda.zip
